import { PendingMedicineService } from './pending-medicine.service';

describe('PendingMedicineService', () => {
  it('should create an instance', () => {
    expect(new PendingMedicineService()).toBeTruthy();
  });
});
